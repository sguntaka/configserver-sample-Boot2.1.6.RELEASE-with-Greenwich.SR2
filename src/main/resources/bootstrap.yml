spring:
  application:
    name: config
  profiles:
    active: default
info:
    build:
        version: #project.version#
management:
  endpoints:
    web:
      exposure:
        include: "*"
  cloudfoundry:
    enabled: false